# 安全政策

## 支持的版本

安全更新仅针对最新发布版本。请及时升级，避免使用过期版本。

| 版本 | 支持状态 |
|------|----------|
| 2.0.x（当前） | 支持 |
| 更早版本 | 不支持，请升级 |

## 报告漏洞

如果你发现了安全漏洞，**请勿公开披露**（不要提交到 Issue/PR）。请通过以下任一渠道私下报告：

- 直接联系维护者（见仓库首页联系方式）
- 在代码托管平台（GitCode/GitHub）私信维护者

报告时请尽量包含：

1. 漏洞类型与影响的组件/模块
2. 触发条件与复现步骤（含最小复现用例）
3. 受影响的版本范围
4. 你建议的修复方案（如有）

## 处理流程

1. 维护者确认漏洞后，会尽快评估影响范围与严重程度
2. 高危漏洞优先修复，并在新版本发布时统一披露
3. 修复完成后，维护者会在 CHANGELOG 中记录

## 安全最佳实践（部署方）

- 生产环境请通过环境变量注入 API Key（`${DASHSCOPE_API_KEY}` 等），勿硬编码进 YAML
- Agent 定义默认遵循最小权限原则：`tools` 按需开放，`PermissionMode` 与审批回调按业务配置
- 不要将 `workspace/`、日志、上传目录等运行时数据暴露到公网
- 依赖版本请保持更新，及时合入安全补丁（Maven Central 依赖由 Dependabot/Renovate 自动跟踪）
